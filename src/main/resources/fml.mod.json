{
  "schemaVersion": 1,
  "id": "${id}",
  "name": "${name}",
  "version": "${version}",
  "description": "${description}",
  "icon": "${icon}",
  "authors": [
    "yuchenxue"
  ],
  "depends": {
    "fishmodloader": ">=3.4.1",
    "kotlin-language": ">=1.0.1"
  },
  "environment": "*",
  "entrypoints": {
    "main": [
      {
        "adapter": "kotlin",
        "value": "cute.neko.kawakaze.KawakazeLib"
      }
    ]
  },
  "mixins": [
    "kawakaze.mixins.json"
  ],
  "accessWidener": "${widener}"
}